node_modules
dist
.next
.env
.DS_Store

# เขียนใหม่ทุกครั้งที่ next dev/build เริ่มทำงาน (ดู next.config.js) — ใช้เทียบเวอร์ชันเช็คว่ามี deploy ใหม่
public/version.json

# ไฟล์สำรองข้อมูล + ไฟล์ตั้งค่าที่มีรหัสผ่านฐานข้อมูล — ห้ามขึ้น git เด็ดขาด
*.dump
*.dump.suspect
backup/projects.conf
backup/*.local.*
.vercel
.env*
# ...แต่ไฟล์ตัวอย่าง (ไม่มีค่าจริง) ต้องขึ้น git เพื่อให้รู้ว่าต้องตั้งค่าอะไรบ้างตอน deploy
!.env.example

# Supabase CLI local state (ไม่ใช่ของโปรเจกต์ ต่างเครื่องต่างค่า)
supabase/.temp/
