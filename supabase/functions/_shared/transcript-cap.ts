// เพดานจำนวนข้อความที่เก็บต่อห้องแชท — ใช้ร่วมกันทุก function ที่เขียน transcript
// ต้องเท่ากันทุกฝั่ง ไม่งั้นฝั่งที่เพดานต่ำกว่าจะตัดประวัติที่อีกฝั่งเก็บไว้ทิ้งเงียบ ๆ
// (เดิม line-webhook ตัดที่ 100, ตอบคอมเมนต์ตัดที่ 80 ทับห้อง Messenger เดียวกัน)
// ห้องที่ยาวที่สุดตอนตั้งค่านี้ ~620 ข้อความ ≈ 20 KB/แถว จึงเผื่อไว้ 1000
export const MAX_TRANSCRIPT_ITEMS = 1000;
